{
  "config": {
    "step": {
      "user": {
        "title": "Choose connection mode",
        "data": {
          "mode": "Mode"
        },
        "data_description": {
          "mode": "Cloud: full functionality with account. Local: sensors only, no internet required."
        }
      },
      "cloud": {
        "title": "Set up Solarmanager Cloud",
        "data": {
          "email": "Email",
          "password": "Password",
          "sm_id": "System ID (smId)",
          "api_key": "Cloud API Key (recommended)"
        },
        "data_description": {
          "sm_id": "The smId of your Solarmanager system from the cloud interface.",
          "api_key": "Cloud API Key for modern authentication (recommended). Found in profile settings under «Cloud API Keys». Create the token without an expiry date, enable all four scopes (read, write, externalOverride:read, externalOverride:write), do NOT enable «Allow renewal». Important: The token is only visible immediately after creation — copy it right away!"
        }
      },
      "local": {
        "title": "Set up local connection",
        "data": {
          "host": "IP address / hostname",
          "scheme": "Protocol",
          "api_key": "API Key (optional)"
        },
        "data_description": {
          "host": "IP address or hostname of the Solar Manager gateway (e.g. 192.168.1.100).",
          "scheme": "HTTP for unencrypted, HTTPS for encrypted (self-signed certificate accepted).",
          "api_key": "Optional API key to secure the local connection. Leave empty if no key is configured."
        }
      },
      "reauth_confirm": {
        "title": "Re-authenticate Solarmanager",
        "description": "Update credentials for system **{sm_id}**.\n\nRecommended: API Key from profile settings → «Cloud API Keys». No expiry date, all four scopes (read, write, externalOverride:read, externalOverride:write), do NOT enable «Allow renewal». The token is only visible immediately after creation — copy it right away!\n\nNote: If the API Key section is not yet visible, contact support — the feature is enabled on request.",
        "data": {
          "email": "Email",
          "password": "Password",
          "api_key": "Cloud API Key (recommended)"
        }
      }
    },
    "error": {
      "auth": "Invalid credentials or API Key. Please check and try again.",
      "cannot_connect": "Connection failed. Is the gateway reachable?",
      "unknown": "Unexpected error. Please check the logs."
    },
    "abort": {
      "already_configured": "This Solarmanager system is already configured.",
      "reauth_successful": "Re-authentication successful. The integration will reload.",
      "reauth_not_supported": "Local mode does not require re-authentication."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Options",
        "description": "Cloud API Key: {api_key_status}",
        "data": {
          "api_key": "Cloud API Key",
          "scan_interval": "Polling interval (seconds)"
        },
        "data_description": {
          "api_key": "Leave empty to keep existing credentials. Enter a new key to switch to API Key authentication or replace the existing one.",
          "scan_interval": "How often the API is polled. Recommended: 10 seconds."
        }
      }
    }
  }
}
