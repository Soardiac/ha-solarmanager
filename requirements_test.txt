pytest-homeassistant-custom-component==0.13.363

# Einzige verbindliche Ruff-Version: die CI liest sie via `version-file` aus
# dieser Datei (siehe .github/workflows/validate.yml), damit lokaler Lint und
# CI nicht auseinanderlaufen. Dependabot haelt sie aktuell.
ruff==0.16.5
